// Type declarations for appReducer.js
import type { AppState, AppAction } from '../types';

export const initialAppState: AppState;
export function appReducer(state: AppState, action: AppAction): AppState;
